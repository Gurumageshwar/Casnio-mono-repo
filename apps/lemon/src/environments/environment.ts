import { AppEnvironment } from '@casino/shared-models';

export const environment: AppEnvironment = {
  production: false,
  apiUrl: 'https://api.lemoncasino.com',
  brand: 'lemon',
  defaultLocale: 'en',
  supportedLocales: ['en']
};
