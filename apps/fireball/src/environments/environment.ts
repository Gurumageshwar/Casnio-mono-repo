import { AppEnvironment } from '@casino/shared-models';

export const environment: AppEnvironment = {
  production: false,
  apiUrl: 'https://api.fireballcasino.com',
  brand: 'fireball',
  defaultLocale: 'en',
  supportedLocales: ['en']
};
