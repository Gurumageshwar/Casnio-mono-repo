import { AppEnvironment } from '@casino/shared-models';

export const environment: AppEnvironment = {
  production: true,
  apiUrl: 'https://api.watchmewin.com',
  brand: 'wmw',
  defaultLocale: 'en',
  supportedLocales: ['en']
};
