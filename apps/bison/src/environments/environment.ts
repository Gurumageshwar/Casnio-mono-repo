import { AppEnvironment } from '@casino/shared-models';

export const environment: AppEnvironment = {
  production: false,
  apiUrl: 'https://api.playbison.com',
  brand: 'bison',
  defaultLocale: 'en',
  supportedLocales: ['en']
};
